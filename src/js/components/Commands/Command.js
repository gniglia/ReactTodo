import React from 'react';

const Command = () => {
  return (
    <div>
      <button>Player 1</button>
      <button>Tie</button>
      <button>Player 2</button>
    </div>
  );
};

export default Command;
